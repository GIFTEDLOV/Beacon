Beacon now authenticates Ethereum/token identity against both CoinGecko and CoinPaprika before accepting semantic sources. Live Bradbury proof verifies canonical USDC and rejects an arbitrary address using the same USDC identifiers. Reassessment gathers every OPEN challenge, preserves each category, reason, and evidence record, evaluates each independently, and resolves only after the new Passport is stored. Two distinct live challenges were opened and included in reassessment. That Bradbury reassessment ended UNDETERMINED because validators returned deterministic violations/timeouts, so no Passport v2 is claimed; atomic readback confirmed neither challenge was partially resolved. Full source, tests, transaction IDs, and readbacks are indexed in the remediation evidence.
